//! 飞书 / 企业微信：身份 provider + 通知 provider + 限频降级（README §4.9、§4.10）。
//!
//! **属于 M5，本里程碑未实现。**
//!
//! 边界由 D5 定死：只做「出站 API 调用」与「浏览器 OAuth 重定向」两类流量，
//! **不暴露任何 IM 事件回调端点**。实现前按 §2.4 的口径重新核对一次平台细节
//! 并把核对日期写回该节——R13 已经写明这条链路组织内无可复用实现。
