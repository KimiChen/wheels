# TODO

## 下一步目标

把 CLI 从“可运行原型”推进到“日常可用工具”：稳定定位微信缓存包，使用项目内置引擎安全解密、解包和反编译，并保留足够的日志和测试来排查问题。

## P0：真实流程验证

- [x] 自动识别小游戏名称，先以 appid、小游戏名称、路径和修改时间作为主要线索。
- [ ] 用 `npm start` 跑一遍完整交互流程，确认候选目录选择体验是否清楚。
- [x] 选择一个已授权的小游戏缓存目录，执行一次真实主包加三个分包反解。
- [x] 检查真实输出的 `game.json`、JavaScript、资源和分包内容，并与旧结果逐文件 SHA-256 对照。
- [ ] 记录内置引擎对主包缺失、分包缺失、解密失败等场景的实际报错。
- [ ] 根据真实报错补充更友好的错误提示。

## P0：安全与误操作防护

- [x] 在执行真实反解前显示更明确的授权用途提示。
- [x] 当输出目录已存在且未传 `--clear` 时，提示用户确认或换目录。
- [x] 对 `--clear` 增加醒目确认，避免误删已有产物。
- [x] dry-run 输出中同时显示工作目录、输入目录和输出目录。
- [x] 包内代码分析 worker 不授予文件写入、网络、子进程或嵌套 worker 权限。
- [x] 由父进程校验并应用内存 manifest，覆盖符号链接、硬链接和路径别名攻击。
- [x] worker 完成消息与请求关联，并隐藏可篡改的进程控制入口。
- [x] 自定义 polyfill 使用父进程私有快照，避免授权可变源目录。
- [x] 在 `--clear` 前准备 polyfill 快照并校验全部 worker 读取授权路径。

## P1：扫描能力增强

- [x] 支持通过 `--appid <wxid>` 过滤候选目录。
- [x] 从路径中提取并展示疑似 appid，例如 `wx0123456789abcdef`。
- [x] 展示每个候选目录内的包清单，包括主包、分包、公共库、大小和修改时间。
- [x] 增加 `--limit <n>`，支持手动限制展示数量，默认显示全部候选。
- [x] 增加 `--since <date>`，只显示指定日期之后修改过的缓存包。
- [x] 同步 macOS、Windows、Linux 默认扫描路径到 README。

## P1：反解执行体验

- [x] 支持选择候选目录后继续选择“完整反解”或“只解包”。
- [x] 反解完成后打印输出目录内的关键文件统计。
- [x] 反解失败时保留内置引擎原始错误，同时给出下一步排查建议。
- [ ] 支持 `--open-dir` 的跨平台验证。
- [x] 增加 `decode --list-packages <target>`，在反解前预览目标目录包列表。

## P1：测试与质量

- [x] 给 `scanWxapkg` 增加 Node.js 单元测试，覆盖主包、分包、publicLib 过滤和排序。
- [x] 给 `defaultOutputDir`、路径展开、字节/时间格式化增加单元测试。
- [x] 用临时目录做一次端到端 dry-run 测试。
- [x] 加入 `npm test`，让 `npm run check` 和单元测试一起跑。
- [x] 在 README 中补充测试命令和成功判定。
- [x] 增加 CLI 版本、帮助和已删除参数的回归测试。
- [x] 增加 VM 错误传播、worker 只读权限、自定义 polyfill 和 manifest 回归测试。
- [x] 增加插件目录映射、输入内容变化、隔离前置、多包顺序和 worker 总超时测试。
- [x] 生成源码中的相对路径统一使用 POSIX 分隔符。
- [ ] 为 `--clear` 增加完整 staging/原子替换，失败时保留上一版输出。
- [ ] 对跨包同路径跳过或覆盖增加汇总提示。

## P2：项目整理与发布

- [x] 增加 `.gitignore`，排除 `node_modules/`、`decoded/`、日志和临时文件。
- [x] 增加 `LICENSE`，与 `wedecode` 的 GPL-3.0-or-later 保持一致。
- [x] 增加 `CHANGELOG.md`，记录后续功能变更。
- [x] 支持通过 `npm link` 在本机全局安装命令。
- [ ] 评估发布为公开或私有 npm 包。
- [x] 审查 `wedecode v0.10.6` 的 GPL-3.0-or-later 许可，并补充源码归属和修改说明。
- [x] 使用 npm `files` 白名单、`prepack` 检查和仓库元数据整理发布包。
- [x] 增加 `DEVELOPMENT.md`，记录目录边界、隔离模型和提交前检查。

## 暂不做

- [x] 不默认联网查询小游戏名称，保留离线线索识别。
