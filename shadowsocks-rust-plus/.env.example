# .env 只接受 KEY=VALUE 字面行，且键必须在 scripts/lib.sh 的允许清单内
# （当前为 UPSTREAM_REPOSITORY 与 CARGO_HOME）。它不会被当作 shell 执行，
# 也不会被发布脚本读取——build-linux-release.sh / sign-release.sh /
# verify-release.sh 一律忽略 .env，避免未跟踪文件影响发布输入。
# 可选：使用本地上游镜像以减少网络下载。不要提交真实凭据。
# UPSTREAM_REPOSITORY=/absolute/path/to/shadowsocks-rust

# 可选：复用 Cargo 下载缓存。
# CARGO_HOME=/absolute/path/to/cargo-home
